{
  "name": "deploy",
  "version": "1.0.0",
  "description": "deploy",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Silak1",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/github": "^5.0.3",
    "@vercel/ncc": "^0.34.0",
    "typescript": "^4.8.3"
  }
}
